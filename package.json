{
  "name": "optcg-sim",
  "version": "0.1.0",
  "private": true,
  "description": "OPTCG multiplayer simulator — pnpm monorepo",
  "scripts": {
    "dev:web": "pnpm --filter @optcg/web dev",
    "build:web": "pnpm --filter @optcg/web build",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test"
  },
  "devDependencies": {
    "typescript": "^5"
  },
  "engines": {
    "node": ">=20",
    "pnpm": ">=9"
  },
  "packageManager": "pnpm@11.1.1"
}
